import express from 'express';
//import { getUsers } from '../controller/userCon.js';
//import { authenticate } from "..//controller/authCon.js";

const router = express.Router();

//router.get('/', authenticate, getUsers);

export default router;